{
  "private": true,
  "scripts": {
    "start": "dotnet fable watch src/Worker -o worker-out & dotnet fable watch src/App --run npx vite",
    "build": "dotnet fable src/Worker -o worker-out && dotnet fable src/App && npx vite build",
    "test:e2e": "npm run build && npx playwright test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^7.3.1"
  }
}
