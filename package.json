{
  "private": true,
  "scripts": {
    "start": "dotnet fable watch worker -o worker-out & dotnet fable watch src --run npx vite",
    "build": "dotnet fable worker -o worker-out && dotnet fable src && npx vite build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
